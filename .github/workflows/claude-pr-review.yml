name: Claude PR Review

on:
  pull_request:
    types: [review_requested, synchronize, opened]

permissions:
  id-token: write
  contents: read
  pull-requests: write
  issues: write

jobs:
  claude-review:
    uses: attentive-mobile/.github/.github/workflows/claude-pr-review.yml@main
    secrets: inherit
